language: node_js
node_js:
  - "stable"
cache:
  npm: false
env:
  - CI=true
install:
  - npm install
script:
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: dist
  on:
    branch: main
